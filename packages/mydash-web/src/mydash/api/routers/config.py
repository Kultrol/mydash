"""User configuration HTTP routes (deferred).

CLI preferences live in
:class:`~mydash.core.services.user_config.UserConfigurationService`
and the ``mydash set`` commands. Config HTTP is not mounted yet
(see :mod:`mydash.api.main`).
"""
