[project]
name = "mydash"
# Single-sourced from src/mydash/__init__.py, which is what `mydash --version`
# reports; see [tool.hatch.version]. Bump it there and nowhere else.
dynamic = ["version"]
description = "Terminal daily dashboard: weather, news, and markets in one brief."
readme = "README.md"
authors = [{ name = "Kevin Medina", email = "physicskev@outlook.com" }]
license = "MIT"
license-files = ["LICENSE"]
keywords = ["cli", "dashboard", "terminal", "weather", "news", "stocks", "tui"]

classifiers = [
  "Development Status :: 5 - Production/Stable",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "Intended Audience :: End Users/Desktop",
  "Natural Language :: English",
  # Tested on Linux and macOS in CI. Windows is unverified: the credentials
  # file's 0600 permissions do not restrict access there.
  "Operating System :: MacOS",
  "Operating System :: POSIX :: Linux",
  "Programming Language :: Python :: 3",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Programming Language :: Python :: 3.14",
  "Topic :: Utilities",
  "Typing :: Typed",
]

requires-python = ">=3.12"
dependencies = [
  "httpx>=0.28.1",
  "platformdirs>=4.11.5",
  "pydantic>=2.13.5",
  "python-dotenv>=1.2.3",
  "rich>=15.0.0",
  "typer>=0.27.2",
]

[project.scripts]
mydash = "mydash.cli.main:app"

[project.urls]
Homepage = "https://github.com/Kultrol/mydash"
Repository = "https://github.com/Kultrol/mydash"
Issues = "https://github.com/Kultrol/mydash/issues"
Changelog = "https://github.com/Kultrol/mydash/blob/main/CHANGELOG.md"

[dependency-groups]
dev = [
  "pytest>=9.1.1",
  "pytest-mock>=3.15.1",
  "ruff>=0.16.5",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.version]
path = "src/mydash/__init__.py"

[tool.hatch.build.targets.wheel]
packages = ["src/mydash"]
# py.typed ships so the "Typing :: Typed" classifier is true for consumers.
artifacts = ["src/mydash/py.typed"]

[tool.hatch.build.targets.sdist]
# Ship what someone needs to build, test, and understand the project.
include = [
  "/src",
  "/test",
  "/docs",
  "/README.md",
  "/CHANGELOG.md",
  "/LICENSE",
  "/.env.example",
]
# The demo screenshot and GIF are most of the sdist by size and nothing
# references them; they belong in the repo, not in every download.
exclude = ["/docs/assets"]

[tool.pytest.ini_options]
testpaths = ["test"]
addopts = "--import-mode=importlib"

[tool.ruff]
line-length = 90
target-version = "py312"
src = ["src", "test"]

[tool.ruff.lint.isort]
# The test suite is a first-party package, not a third-party "test" module.
known-first-party = ["mydash", "test"]

[tool.ruff.lint]
select = ["E", "W", "F", "I", "B", "SIM", "UP"]
ignore = [
  # Panels and forecasts are full of intentional emoji, en dashes, and °C.
  "RUF001",
  "RUF002",
  "RUF003",
]
