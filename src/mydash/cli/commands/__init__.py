"""Per-command Typer handlers (target layout).

Today all commands live in cli/main.py and call client factories inline. Whether to
split one module per command or group by domain is still open — this package is a
placeholder for that exploration.
"""