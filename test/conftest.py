"""Shared pytest fixtures for mydash tests.

Provider and service tests currently define their own sample data and mocks.
Add shared fixtures here when the same payloads are reused across packages.
"""
